{
  "name": "hermes-workspace-os",
  "version": "0.1.0",
  "private": true,
  "description": "Hermes Workspace OS — self-hostable AI Workspace Operating System (JS/TS workspace root)",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter @hermes/web dev",
    "build": "pnpm --filter @hermes/web build",
    "lint": "pnpm --filter @hermes/web lint",
    "typecheck": "pnpm --filter @hermes/web typecheck",
    "test": "pnpm --filter @hermes/web test"
  },
  "engines": {
    "node": ">=20"
  }
}
